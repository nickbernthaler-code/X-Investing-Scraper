# System files
.DS_Store
Thumbs.db

# Environment variables (keep API keys safe!)
.env
.env.local

# Node modules (if we add any packages later)
node_modules/

# Database files — contain user data, don't commit
*.db
*.sqlite
db.json

# Editor settings
.vscode/
.idea/
